# Vue3 全栈项目进阶拓展需求文档

## 文档说明

本文档面向已完成基础项目的学生，旨在通过**AI辅助编程**的方式，按照**企业级开发标准**，系统性地完善电商平台。每个需求都配有**AI 提示词（Prompt）示例**，帮助学生借助 AI 工具高效完成开发。

***

## 一、核心购物流程完善

### 1.1 订单系统（优先级：⭐⭐⭐⭐⭐）

| 页面/功能 | 功能描述                         | 涉及数据表                                |
| ----- | ---------------------------- | ------------------------------------ |
| 结算页   | 确认收货地址、商品清单、支付方式、优惠券         | `orders`, `order_items`, `addresses` |
| 订单列表  | 按状态分类展示（待付款/待发货/待收货/已完成/已取消） | `orders`                             |
| 订单详情  | 展示完整订单信息、物流状态、售后入口           | `orders`, `order_items`, `logistics` |
| 订单操作  | 取消订单、确认收货、申请退货               | `orders`, `returns`                  |

**AI 提示词示例(仅供参考，需自行调整)：**

```
"使用 Vue3 + Pinia 实现订单结算页面，包含：
1. 收货地址选择（默认地址高亮）
2. 商品清单展示（从购物车数据读取）
3. 订单金额汇总（商品总价 + 运费 - 优惠）
4. 提交订单按钮（调用后端 /api/orders 接口）
5. 支付方式选择（模拟：微信/支付宝/银行卡）
请生成完整的 Vue 组件代码和对应的 Pinia store"
```


### 1.2 收货地址管理（优先级：⭐⭐⭐⭐⭐）

| 页面/功能   | 功能描述             | 涉及数据表       |
| ------- | ---------------- | ----------- |
| 地址列表    | 展示所有收货地址，默认地址标记  | `addresses` |
| 新增/编辑地址 | 收货人、手机号、省市区、详细地址 | `addresses` |
| 地址删除    | 软删除地址            | `addresses` |
| 设为默认    | 切换默认地址           | `addresses` |

**AI 提示词示例(仅供参考，需自行调整)：**

```
"使用 Vue3 实现收货地址管理功能：
1. 地址列表页面（卡片展示，默认地址带标签）
2. 新增/编辑地址表单（包含：收货人、手机号、省市区选择器、详细地址）
3. 删除地址（二次确认弹窗）
4. 设为默认地址功能
需要包含完整的表单校验（手机号格式、必填项）和 API 调用"
```


***

## 二、后端数据持久化（MySQL）

### 2.1 数据库设计（优先级：⭐⭐⭐⭐⭐）

**技术选型：** MySQL 

**数据表设计（仅供参考，需自行调整）：**

```sql
-- 用户表
CREATE TABLE users (
  id INT PRIMARY KEY AUTO_INCREMENT,
  username VARCHAR(50) UNIQUE NOT NULL,
  password VARCHAR(255) NOT NULL,  -- bcrypt 加密
  name VARCHAR(50),
  email VARCHAR(100),
  avatar VARCHAR(255),
  role ENUM('admin', 'user') DEFAULT 'user',
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
  updated_at DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
);

-- 商品表
CREATE TABLE products (
  id INT PRIMARY KEY AUTO_INCREMENT,
  name VARCHAR(100) NOT NULL,
  price DECIMAL(10,2) NOT NULL,
  description TEXT,
  category VARCHAR(50),
  image VARCHAR(255),
  stock INT DEFAULT 0,
  rating DECIMAL(3,2) DEFAULT 0,
  sales INT DEFAULT 0,  -- 销量
  status TINYINT DEFAULT 1,  -- 1:上架 0:下架
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
  updated_at DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
);

-- 购物车表
CREATE TABLE cart_items (
  id INT PRIMARY KEY AUTO_INCREMENT,
  user_id INT NOT NULL,
  product_id INT NOT NULL,
  quantity INT DEFAULT 1,
  selected TINYINT DEFAULT 1,  -- 是否选中
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
  updated_at DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE,
  FOREIGN KEY (product_id) REFERENCES products(id) ON DELETE CASCADE,
  UNIQUE KEY unique_cart (user_id, product_id)
);

-- 订单表
CREATE TABLE orders (
  id INT PRIMARY KEY AUTO_INCREMENT,
  order_no VARCHAR(32) UNIQUE NOT NULL,  -- 订单编号
  user_id INT NOT NULL,
  total_amount DECIMAL(10,2) NOT NULL,
  pay_amount DECIMAL(10,2) NOT NULL,
  freight DECIMAL(10,2) DEFAULT 0,
  status ENUM('pending', 'paid', 'shipped', 'completed', 'cancelled') DEFAULT 'pending',
  pay_method ENUM('wechat', 'alipay', 'bank') DEFAULT 'wechat',
  address_id INT NOT NULL,
  remark VARCHAR(255),
  paid_at DATETIME,
  shipped_at DATETIME,
  completed_at DATETIME,
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
  updated_at DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  FOREIGN KEY (user_id) REFERENCES users(id),
  FOREIGN KEY (address_id) REFERENCES addresses(id)
);

-- 订单商品明细表
CREATE TABLE order_items (
  id INT PRIMARY KEY AUTO_INCREMENT,
  order_id INT NOT NULL,
  product_id INT NOT NULL,
  product_name VARCHAR(100) NOT NULL,
  product_price DECIMAL(10,2) NOT NULL,
  quantity INT NOT NULL,
  total_price DECIMAL(10,2) NOT NULL,
  FOREIGN KEY (order_id) REFERENCES orders(id) ON DELETE CASCADE,
  FOREIGN KEY (product_id) REFERENCES products(id)
);

-- 地址表
CREATE TABLE addresses (
  id INT PRIMARY KEY AUTO_INCREMENT,
  user_id INT NOT NULL,
  receiver VARCHAR(50) NOT NULL,
  phone VARCHAR(20) NOT NULL,
  province VARCHAR(50) NOT NULL,
  city VARCHAR(50) NOT NULL,
  district VARCHAR(50) NOT NULL,
  detail VARCHAR(255) NOT NULL,
  is_default TINYINT DEFAULT 0,
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
  updated_at DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE
);

-- 商品评价表
CREATE TABLE reviews (
  id INT PRIMARY KEY AUTO_INCREMENT,
  user_id INT NOT NULL,
  product_id INT NOT NULL,
  order_id INT NOT NULL,
  rating TINYINT NOT NULL,  -- 1-5
  content TEXT,
  images JSON,  -- 评价图片
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
  updated_at DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  FOREIGN KEY (user_id) REFERENCES users(id),
  FOREIGN KEY (product_id) REFERENCES products(id),
  FOREIGN KEY (order_id) REFERENCES orders(id)
);
```


### 2.2 后端接口升级

**AI 提示词示例：**

```
"使用 Node.js + Express + Sequelize(MySQL) 实现以下接口：
1. POST /api/orders - 创建订单（事务处理：扣减库存、清空购物车）
2. GET /api/orders - 获取订单列表（支持状态筛选、分页）
3. GET /api/orders/:id - 获取订单详情
4. PUT /api/orders/:id/cancel - 取消订单
5. PUT /api/orders/:id/status - 更新订单状态（管理员）
```


***

## 三、管理后台（优先级：⭐⭐⭐⭐）

### 3.1 管理后台功能模块

| 模块       | 功能                    | 权限    |
| -------- | --------------------- | ----- |
| **仪表盘**  | 数据概览（销售额、订单量、用户数、商品数） | admin |
| **商品管理** | 商品列表、新增/编辑/删除、上下架     | admin |
| **订单管理** | 订单列表、订单详情、发货/取消操作     | admin |
| **用户管理** | 用户列表、禁用/启用、查看详情       | admin |
| **数据统计** | 销售趋势图表、分类占比、热销排行      | admin |

### 3.2 管理后台技术实现

**路由结构：**

```
/admin
  ├── dashboard         # 仪表盘
  ├── products          # 商品管理
  │   ├── list          # 商品列表
  │   ├── create        # 新增商品
  │   └── edit/:id      # 编辑商品
  ├── orders            # 订单管理
  │   ├── list          # 订单列表
  │   └── detail/:id    # 订单详情
  ├── users             # 用户管理
  └── statistics        # 数据统计
```


**AI 提示词示例：**

```
"使用 Vue3 + Element Plus 实现管理后台的商品管理页面：
1. 商品列表（表格展示：ID、名称、价格、库存、状态、操作按钮）
2. 搜索筛选（按名称、分类、状态）
3. 分页功能
4. 新增/编辑商品弹窗（表单：名称、价格、描述、分类、图片、库存）
5. 上下架切换开关
6. 删除商品（二次确认）
要求：使用 Pinia 管理商品状态，调用 /api/admin/products 接口"
```


***

## 四、用户体验优化

### 4.1 页面加载体验

| 优化项   | 实现方式                           |
| ----- | ------------------------------ |
| 骨架屏   | 数据加载时展示骨架屏替代 Loading 动画        |
| 懒加载   | 商品图片使用 `v-lazy` 懒加载            |
| 无限滚动  | 商品列表滚动加载更多                     |
| 路由预加载 | 使用 `router.beforeEach` 预加载关键数据 |

**AI 提示词示例：**

```
"为 Vue3 商品列表页面实现骨架屏效果：
1. 加载时显示 6 个卡片骨架（灰色块模拟图片、标题、价格）
2. 数据加载完成后平滑切换到真实内容
3. 使用 CSS 动画实现呼吸光效
请生成完整的 Vue 组件代码"
```


### 4.2 交互反馈优化

| 优化项      | 实现方式                           |
| -------- | ------------------------------ |
| Toast 提示 | 统一使用 `Element Plus` 或自定义 Toast |
| 加载状态     | 按钮点击防抖、提交中状态禁用                 |
| 确认弹窗     | 删除、取消订单等操作二次确认                 |
| 表单校验     | 实时校验、提交前完整校验                   |

### 4.3 响应式适配

| 设备   | 适配要求                 |
| ---- | -------------------- |
| PC 端 | 1200px 以上，多列网格布局     |
| 平板   | 768px - 1200px，两列布局  |
| 移动端  | 768px 以下，单列布局 + 底部导航 |

***

## 五、高级功能拓展

### 5.1 搜索与筛选

| 功能    | 描述            |
| ----- | ------------- |
| 关键词搜索 | 按商品名称、描述搜索    |
| 分类筛选  | 按商品分类过滤       |
| 价格区间  | 按价格范围筛选       |
| 排序    | 综合/价格/销量/评价排序 |

### 5.2 商品收藏

| 功能      | 描述           |
| ------- | ------------ |
| 收藏/取消收藏 | 商品详情页点击心形图标  |
| 收藏列表    | 个人中心查看所有收藏商品 |

**涉及数据表：** `favorites (id, user_id, product_id, created_at)`

### 5.3 商品评价

| 功能   | 描述                |
| ---- | ----------------- |
| 发表评价 | 订单完成后对商品进行评分和文字评价 |
| 评价展示 | 商品详情页展示用户评价列表     |
| 评价统计 | 显示好评率、各星级占比       |

### 5.4 优惠券系统

| 功能    | 描述         |
| ----- | ---------- |
| 优惠券类型 | 满减券、折扣券    |
| 领取优惠券 | 在优惠券中心领取   |
| 使用优惠券 | 结算时选择可用优惠券 |

**涉及数据表：** `coupons`, `user_coupons`

***

***

## 七、项目扩展方向总览

### 7.1 按技术深度分级

| 级别    | 方向                 | 难度    |
| ----- | ------------------ | ----- |
| 🌱 基础 | 完整购物流程（订单、地址、支付模拟） | ⭐⭐⭐   |
| 🌿 进阶 | 管理后台 + 数据库持久化      | ⭐⭐⭐⭐  |
| 🌳 高级 | 评价系统 + 优惠券 + 搜索    | ⭐⭐⭐⭐⭐ |

### 7.2 按功能模块分组

```
电商平台功能矩阵
├── 用户端
│   ├── 商品浏览（列表/详情/搜索/筛选/收藏）
│   ├── 购物车（增删改/选中/批量操作）
│   ├── 订单流程（下单/支付/取消/确认收货）
│   ├── 个人中心（信息/地址/订单/收藏/评价）
│   └── 用户交互（登录/注册/密码重置）
├── 管理端
│   ├── 商品管理（CRUD/上下架/库存）
│   ├── 订单管理（列表/发货/状态变更）
│   ├── 用户管理（列表/禁用/角色）
│   └── 数据统计（销售额/用户/商品/订单）
└── 基础设施
    ├── MySQL 数据库（7+ 张表）
    ├── 接口认证（JWT）
    ├── 文件上传（商品图片）
    └── 日志记录（操作日志/错误日志）
```


***

## 八、AI 使用指南

### 8.1 高效提问模板

```
【技术栈】Vue3 + Pinia + Vite + Axios
【功能需求】描述具体要做什么
【接口规范】后端接口格式
【UI 要求】页面布局、交互细节
【约束条件】需要考虑的边界情况
```


### 8.2 代码审查提示

```
"请审查以下代码，指出：
1. 潜在的性能问题
2. 类型安全问题
3. 代码可读性改进
4. 最佳实践建议"
```


### 8.3 错误排查提示

```
"我在使用 [功能] 时遇到 [错误信息]，
当前环境：[Node版本/NPM版本]
相关代码：[粘贴关键代码]
请帮我定位问题并给出解决方案"
```


***

## 九、交付标准

### 9.1 功能完整性

* [ ] 所有页面路由配置完整

* [ ] 所有接口调佣正常

* [ ] 数据流（前端 → 后端 → 数据库）完整打通

### 9.2 代码质量

* [ ] 组件拆分合理（复用性）

* [ ] 命名规范统一

* [ ] 注释完整（复杂逻辑）

### 9.3 用户体验

* [ ] 加载状态反馈

* [ ] 错误提示友好

* [ ] 表单校验完整

* [ ] 页面响应式适配

### 9.4 文档输出

* [ ] README.md（项目简介/启动方式/技术栈）

* [ ] API 文档（接口列表/请求参数/响应示例）

* [ ] 数据库设计文档（ER 图/表结构说明）

* [ ] 部署文档（环境要求/部署步骤）

***

## 📌 项目完善路线图

```
方向1：核心购物流程（2周）
├── 收货地址管理（增删改查/默认地址）
├── 订单创建（从购物车生成订单）
├── 订单列表（状态筛选/分页）
└── 订单详情

方向2：数据持久化（1周）
├── MySQL 数据库安装与配置
├── Sequelize/Prisma 连接配置
├── 数据表创建与迁移
└── 接口改造（从内存数据 → 数据库）

方向3：管理后台（2周）
├── 商品管理（CRUD/上下架）
├── 订单管理（列表/发货/状态变更）
└── 用户管理（列表/禁用/角色）

方向4：高级功能（2周）
├── 商品搜索/筛选/排序
├── 商品收藏
├── 商品评价
└── 数据统计（ECharts 图表）
```


***

> 💡 **建议：** 学生可根据自身水平选择1个方向或者某个方向的1-2个功能模块深入，不必面面俱到。重点是**利用 AI 辅助快速实现功能**，同时在实践中理解**前后端完整交互流程**。

